---
# Install with: ansible-galaxy collection install -r requirements.yml
# The control node also needs the PyMySQL driver: pip install PyMySQL
collections:
  - name: community.mysql
    version: ">=3.9.0"
