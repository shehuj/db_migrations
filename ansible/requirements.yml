---
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: amazon.aws
    version: ">=7.0.0"
  # Canonical successor to community.mysql (ansible-namespace migration).
  - name: ansible.mysql
    version: ">=5.0.0"
