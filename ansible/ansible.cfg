[defaults]
inventory            = inventory/hosts.yml
roles_path           = roles
collections_path     = collections
host_key_checking    = False
retry_files_enabled  = False
stdout_callback      = yaml
deprecation_warnings = False
# Ansible runs on the control node; the MySQL modules connect over TCP to the
# RDS endpoints (dev directly, prod through the SSM port-forward on localhost).
